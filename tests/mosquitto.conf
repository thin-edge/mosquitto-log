# Minimal broker config for the plugin functional tests.
# The plugin is mounted at /plugin/logger.so and logs to $MQTT_LOG_DIR
# (set via the container environment).
listener 1883
allow_anonymous true

plugin /plugin/logger.so
